# AGENTS: ikeisuke.github.io 運用方針（全体方針）

このリポジトリは GitHub Pages（ユーザーサイト）で、各アプリのポリシーや簡易ドキュメントを一元的に公開するためのベースです。以下の方針・規約に従って作業してください。

## 目的と範囲
- 目的: アプリごとのプライバシーポリシー等を安定URLで公開・維持する。
- 範囲: `apps/<app>/...` 配下にアプリ固有ページ（例: プライバシーポリシー）を配置する。
- 既存別レポジトリのドキュメント移管（例: MimiLoopDocs）は、個別Issue/PRで段階的に実施（本PRではTumbliumのみ）。

## 技術スタックとビルド
- GitHub Pages（ユーザーサイト）+ Jekyll（テーマ依存最小）
- ルートに `_config.yml` を置き、`_layouts/` で共通レイアウト、`_data/` でアプリ一覧を管理。
- 外部依存は増やさず、静的Markdown中心。JS/CSSは必要最小限に留める。

## 情報設計とURL設計
- アプリ別ポリシー: `apps/<app>/privacy/index.md`（末尾スラッシュのプレティURL）
- トップ: `index.md` で `_data/apps.yml` を元に一覧生成。
- 既存URLは極力固定（パス変更が必要な場合はリダイレクト案内をページ内で実施）。

## コンテンツ方針（日本語基準）
- まず日本語で明確・簡潔に。必要に応じて英語版を別途検討。
- プライバシーポリシーは以下の章立てを推奨：
  1) 収集情報 2) 収集方法 3) 利用目的 4) 保存・共有 5) 第三者提供
  6) 外部サービス連携 7) 分析/クラッシュ 8) ユーザー権利 9) セキュリティ
  10) 未成年 11) 変更 12) お問い合わせ
- 実装確定後に具体的項目（SDK名・送信先等）を追記。未確定箇所は曖昧表現を避け、「暫定/追記予定」を明示。

## 追加・更新手順（開発者向け）
1. 新規アプリのページを作成: `apps/<app>/privacy/index.md`
   - Front Matter: `layout: default`, `title: <App> プライバシーポリシー`
   - 既存雛形（`apps/tumblium/privacy/index.md`）を参考に執筆。
2. 一覧へ登録: `_data/apps.yml` に `id/name/links.privacy` を追加。
3. 連絡先設定（任意）: `_config.yml` の `contact.email` を更新。
4. レビュー観点: 章立て、URL、表現の明確性、アプリ固有事項の反映、法令上の表現妥当性。

## 禁止・注意事項
- 動的追跡や外部スクリプトを無断追加しない（ポリシー整合・同意設計が先）。
- 大規模テーマ導入・ビルド依存追加は要事前合意。
- 他アプリ移管は別Issue/PRで（本リポジトリの目的外変更を混在させない）。

## 本PRの対象
- 対象: Tumblium のプライバシーポリシー公開基盤整備とページ追加のみ。
- 非対象: MimiLoop 等の移管（今後別途対応）。

